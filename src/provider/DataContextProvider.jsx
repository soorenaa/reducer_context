import { AppContext } from "../contexts/imageListContext";

AppContext

const Provider = ({ chidren }) => {
    <AppContext.Provider  >
        {chidren}
    </AppContext.Provider>
}

export default Provider;